name: Catch Secrets
on:
  repository_dispatch:
    types:
      - webhook

jobs:
  catch-secrets:
    runs-on: ubuntu-latest
    steps:
      - name: See the info received
        run: |
          echo "The event name is ${{ github.event.action }}"
          echo "The event payload is ${{ github.event.client_payload }}"